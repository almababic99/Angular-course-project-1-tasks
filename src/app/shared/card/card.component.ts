import { Component } from '@angular/core';

@Component({
  selector: 'app-card',
  // standalone: true,  // we don't need this beacause of using ngmodules instead of standalone
  // imports: [],   // we don't need this beacause of using ngmodules instead of standalone
  templateUrl: './card.component.html',
  styleUrl: './card.component.css'
})
export class CardComponent {

}
